name: Eighth Workflow
on: workflow_dispatch
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Command in multiline
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
